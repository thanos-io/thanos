apiVersion: v1
kind: Service
metadata:
  name: thanos-ruler
  namespace: default
  labels:
    app: thanos-ruler
spec:
  type: NodePort
  selector:
    app: thanos-ruler
  ports:
  - name: http
    port: 9094
    targetPort: http
    nodePort: 30094
